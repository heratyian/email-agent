# Model credentials (only the provider selected by a profile is required)
OPENAI_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# Gmail OAuth files are deliberately kept outside source control
GMAIL_CREDENTIALS_FILE=secrets/gmail_credentials.json
GMAIL_TOKEN_FILE=secrets/gmail_token.json

# Private Email credentials
RECEIPT_AI_EMAIL_USERNAME=support@receipt-ai.com
RECEIPT_AI_EMAIL_PASSWORD=

EMAIL_AGENT_DATABASE=data/email_agent.db

